package view;

import javafx.scene.paint.Color;
import javafx.scene.shape.Rectangle;

public class BigShape extends Rectangle {

    public BigShape(){
        super();
        super.setWidth(500);
        super.setHeight(500);
        super.setFill(Color.GREEN);
    }
}
